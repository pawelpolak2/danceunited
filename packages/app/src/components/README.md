# Components Directory

This directory contains reusable React components that can be used across the application.

## Structure

- `ui/` - Basic UI components (buttons, inputs, cards, etc.)
- `layout/` - Layout-related components (headers, footers, containers)
- `features/` - Feature-specific components

